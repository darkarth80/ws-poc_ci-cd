server:
  servlet:
    context-path: /ws-poc
  port: 5001

#TODO: figure out why I need this here and in bootstrap.yml
spring:
  application:
    name: testLatticeApp

ribbon:
  ServerListRefreshInterval: 1000

endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true

app:
  paises:
    default:
      pais: MX
      oficina: Azcapotzalco
      informacion: Oficinas MX
